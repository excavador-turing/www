# BMC-Firmware

The firmware image — what you flash onto the board.

Newest release **v2.24.0**, 21 in total. Every entry is the release note as published on [GitHub](https://github.com/excavador-turing/BMC-Firmware/releases), fetched by `just refresh-changelog` — not written here, so the two cannot disagree.

???+ note "v2.24.0 — 11 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.23.0 — 11 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.22.0 — 11 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.21.0 — 11 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.20.0 — 10 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.19.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.18.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.17.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.16.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.15.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.14.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.13.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.12.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

    `turingpi-bmc-dashboard.json` is a Grafana dashboard over the
    metrics this build's daemon emits. Import it and pick your
    Prometheus-compatible datasource; see
    https://turing.excavador.xyz/guides/monitor-it/

??? note "v2.11.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

??? note "v2.10.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

??? note "v2.9.2 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

??? note "v2.9.1 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

??? note "v2.9.0 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

??? note "v2.8.1 — 09 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

??? note "v2.8.0 — 08 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.

??? note "v2.7.0 — 08 September 2026"

    Turing Pi 2 BMC firmware, built from the `hive` branch.

    **Not a Turing Pi release.** Upstream is dormant: last release
    v2.1.0 (2025-02-05), last commit 2025-08-28, and the lead
    maintainer stated on 2026-06-16 that he has moved on.

    Includes the unmerged versioning fix (upstream PR #242), so
    `/etc/os-release` reports the actual firmware version instead of
    Buildroot's, and bmcd moves v2.3.4 -> v2.3.7.

    `.tpu` is the OTA package, `.img` the recovery SD image.
    Verify with `sha256sum -c SHA256SUMS`.
