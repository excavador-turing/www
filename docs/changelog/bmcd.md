# bmcd

The daemon: the API, the update logic, the metrics.

Newest release **v2.36.3**, 33 in total. Every entry is the release note as published on [GitHub](https://github.com/excavador-turing/bmcd/releases), fetched by `just refresh-changelog` — not written here, so the two cannot disagree.

???+ note "v2.36.3 — 12 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.36.1 — 12 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.36.0 — 12 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.35.0 — 11 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.34.0 — 11 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.33.0 — 11 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.32.0 — 11 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.31.0 — 11 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.30.0 — 11 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.29.0 — 10 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.28.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.27.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.26.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    `openapi.json` is this daemon's own description of its API,
    emitted by the binary that implements it, so it describes this
    release and no other. A board serves the same document at
    `GET /api/bmc/openapi.json`.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.25.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.24.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.23.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.22.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.21.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.19.1 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.20.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.19.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.18.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.17.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.16.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.15.1 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.15.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.14.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.13.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.12.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.11.0 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.10.1 — 09 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.10.0 — 08 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.

??? note "v2.9.0 — 08 September 2026"

    bmcd built from the `hive` branch of this fork, for the Turing Pi 2
    BMC firmware build to consume.

    **Not a Turing Pi release.** The firmware pins this repository by
    commit and compiles it inside Buildroot, so no binary is published
    here -- a binary built by this runner would not match the board's
    toolchain. The tarball is the source that was tagged.

    Verify with:

    ```
    sha256sum -c SHA256SUMS
    ```

    SHA256SUMS lists bare filenames, so run it from the directory
    holding the tarball.
