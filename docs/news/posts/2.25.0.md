---
title: "Firmware v2.25.0"
date: 2026-09-11
slug: v2.25.0
---

Firmware v2.25.0.

<!-- more -->

**Firmware v2.25.0**, 11 September 2026. Every item below is the repository's own changelog entry, taken apart; the whole entry is a click away under each one.

## Changed

- **tpi → v1.7.0.** , which releases the fix already on the branch: renaming a board no longer claims to move its metrics history, because the series never carried a board name.

    ??? note "The whole entry"

        Bumped because the pin check added in v2.24.0 caught it — it flagged tpi as
        stale the moment the interface was bumped, which is exactly the drift it was
        written for, on its first real use.


## Fixed

- **BMC-UI v3.23.0 → v3.24.0: the firmware install confirmation is a modal.** It rendered inline, after every candidate.

    ??? note "The whole entry"

        A board with several sources has a long catalogue, so pressing INSTALL scrolled the question off the bottom of the window — the button appeared to do nothing, and the obvious response is to press it again. It now uses the same modal the reboot and node-power confirmations use, and its drawer form fixes the phone case the inline panel was worst on. Reported from use.


---

[Every release of the firmware](../../changelog/firmware.md) · [the roadmap](../../roadmap.md) · [follow by feed](../../feed.xml)
